/*

1、hp = hp2，hp2是一个左值，rhs将使用拷贝构造函数来初始化，拷贝构造函数将分配一个新的string，并拷贝hp2指向的string。

2、hp = std::move(hp2)，我们调用std::move将一个右值引用绑定到hp2上。在此情况下，拷贝构造函数和移动构造函数都是可行的。
但是，由于实参是一个右值引用，移动构造函数是精确匹配的。移动构造函数从hp2拷贝指针，而不会分配内存

*/