//a : int i, *const cp = &i;

//b : int *pl, *const p2 = pl;

//c : const int ic = 1, &r = ic;

//d : int i; const int *cosnt p3 = i;

//& 必须引用一个值
//const int 必须初始化
//cosnt int & 必须引用一个值，可以不初始化
//const int * 可以不指向任何值，如值向的值可以不初始化
//int *const 必须指向一个值，可以不初始化
//const int *const 必须指向一个值，可以不初始化