/*

a : 
do
{
    int v1, v2;
    
    cout << "Please enter two numbers to sum  : ";
    if(cin >> v1 >> v2)
        cout << "Sum is : " << v1 + v2 << endl;
}while (cin); //输入非数字退出循环

b :
int ival;

do
{
    //
}while(ival == get_response());  // ival == get_response() 退出循环

c : 
int ival;

do
{
    ival = get_response();
}while (ival);  // get_response()返回0结束循环

*/